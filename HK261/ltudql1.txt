Tuần 1-4:
- Public Class Form1 ... End Class → Khai báo lớp Form1 (form giao diện chính).
- Private Sub ... Handles ... → Thủ tục xử lý sự kiện (event handler), ví dụ: Handles BDong.Click chạy khi bấm nút BDong.
- Dim a, b, c As Double → Khai báo nhiều biến kiểu số thực trên cùng một dòng.
- Dim hople As Boolean → Biến kiểu logic (True/False).
- Double.TryParse(TBA.Text, a) → Chuyển chuỗi sang số thực, trả về True nếu thành công.
- If ... Then ... ElseIf ... Else ... End If → Cấu trúc rẽ nhánh điều kiện.
- ByRef → Tham số truyền tham chiếu, giá trị thay đổi trong hàm sẽ ảnh hưởng ra ngoài.
- String.Format("...", x1, x2) → Chèn giá trị biến vào chuỗi theo định dạng {0}, {1}.
- Math.Sqrt(delta) → Hàm căn bậc hai.

- Khi tạo button, Text là hiển thị, Name mới là tên biến, double click vào sẽ hiển thị hàm để code

Tuần 5:
- Để code cho 1 sự kiện (của 1 button), chọn properties, chọn đúp vào sự kiện (vd FormClosing) -> gen code
	Dim dr as DialogResult	= MessageBox.Show("Do u wanna close","Title",MessageBoxButtons.OKCancel)
	If dr == DialogResult.Cancel Then
		e.Cancel = True
	End If
- Chọn Designer.vb để xem design của form

Tuần 8:
- (Name) properties sẽ là tên biến

Để liên kết csld (access) thì sử dụng 1 file module XL_DuLieu có 2 phương thức như sau:
	Imports System.Data.OleDb

	Module XL_DuLieu

		Dim Chuoi_Ket_Noi = "Provider=Microsoft.ACE.OLEDB.12.0;Data Source=CuaHangTapHoa.accdb"

		Public Function DocDuLieu(ByVal sql As String) As DataTable
			Dim kq As DataTable = New DataTable()
			Dim KetNoi As OleDbConnection = New OleDbConnection(Chuoi_Ket_Noi)
			Dim CauTruyVan As OleDbCommand = New OleDbCommand(sql, KetNoi)
			Dim DongBo As OleDbDataAdapter = New OleDbDataAdapter(CauTruyVan)
			DongBo.Fill(kq)
			Return kq
		End Function

		Public Function GhiDuLieu(ByVal TenBang As String, ByVal dt As DataTable) As Integer
			Dim kq As DataTable = New DataTable()
			Dim KetNoi As OleDbConnection = New OleDbConnection(Chuoi_Ket_Noi)
			Dim CauTruyVan As OleDbCommand = New OleDbCommand("Select * from " + TenBang, KetNoi)
			Dim DongBo As OleDbDataAdapter = New OleDbDataAdapter(CauTruyVan)
			Dim builder As OleDbCommandBuilder = New OleDbCommandBuilder(DongBo)

			AddHandler DongBo.RowUpdated, AddressOf CapNhatMaSo

			Return DongBo.Update(dt) 'Insert, Update, Delete
		End Function

		Private Sub CapNhatMaSo(ByVal sender As Object, ByVal e As OleDbRowUpdatedEventArgs)
			If e.StatementType = StatementType.Insert Then
				Dim KetNoi As OleDbConnection = e.Command.Connection
				Dim CauTruyVan As OleDbCommand = New OleDbCommand("Select @@IDENTITY", KetNoi)
				Dim MaSo As Integer = Integer.Parse(CauTruyVan.ExecuteScalar())
				e.Row(0) = MaSo
			End If
		End Sub

	End Module

Sau đó vào nuget tải oledatabase về, lúc này khi xài chỉ cần XL_DuLieu.DocDuLieu và XL_DuLieu.GhiDuLieu

Để tạo UI theo khung thì sử dụng ToolBox Panel gồm 3 loại, thường sẽ sử dụng table layout panel và panel. Các bảng lớn thì để vào table layout, xong sử dụng Dock -> Fill để chiếm trọn màn hình. Với Panel thì cho nó tràn ra bằng thuộc tính Collumn Span/Row Span (click vào panel chứ không phải table layout). Để nó nằm giữa thì chọn Anchor -> tắt Top, Left đi